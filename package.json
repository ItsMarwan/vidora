{
  "name": "vidora",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Vidora — server-side TMDB proxy + server-relayed Watch Party on Vercel.",
  "scripts": {
    "dev": "node dev-server.js",
    "dev:vercel": "npx vercel dev --listen 3000 --local"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0"
  }
}
